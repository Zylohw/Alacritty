# Alacritty config generado a partir de settings.json de Windows Terminal
# Perfil base: MaterialOcean, Hack Nerd Font Mono, opacidad 90%

[window]
opacity = 0.9
padding = { x = 8, y = 8 }
decorations = "full"
dimensions = { columns = 120, lines = 30 }

[font]
size = 15.0

[font.normal]
family = "Hack Nerd Font Mono"
style = "Regular"

[font.bold]
family = "Hack Nerd Font Mono"
style = "Bold"

[font.italic]
family = "Hack Nerd Font Mono"
style = "Italic"

# Directorio de inicio explícito, para no heredar System32 del proceso padre (explorer.exe)
# El "import" carga el tema activo. Cambia esta ruta para intercambiar de tema
# (o hazlo con un script que reemplace esta línea).
[general]
working_directory = 'C:\Users\pvasq'
import = ["themes/material_ocena.toml"]

# Shell por defecto en Windows -> PowerShell (ruta exacta del usuario)
[terminal.shell]
program = 'C:\Users\pvasq\AppData\Local\Microsoft\WindowsApps\Microsoft.PowerShell_8wekyb3d8bbwe\pwsh.exe'
args = ["-NoLogo"]

[cursor]
style = { shape = "Beam", blinking = "On" }

# Los colores ahora viven en themes/*.toml, cargados vía [general] import arriba
[colors]
draw_bold_text_with_bright_colors = true

[keyboard]
bindings = [
  { key = "C", mods = "Control", action = "Copy" },
  { key = "V", mods = "Control", action = "Paste" },
  { key = "F", mods = "Control|Shift", action = "SearchForward" },
]